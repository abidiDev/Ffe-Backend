server:
  port: 8889

spring:
  application:
    name: "cloudgateway"
  cloud:
    gateway:
      global-cors:
        corsConfigurations:
          '[/**]':
            allowedOrigins: "*"
            allowedMethods: "*"
